T=3;
t=0:0.002:3*T;
y=0.8*sin(2*pi*1/3*t);
y(y<0)=0;
subplot(3,1,1)
plot(t,y)
grid on
t=0:0.02:3*T;
y=0.8*sin(2*pi*1/3*t);
y(y<0)=0;
subplot(3,1,2)
plot(t,y)
grid on
t=0:0.2:3*T;
y=0.8*sin(2*pi*1/3*t);
y(y<0)=0;
subplot(3,1,3)
plot(t,y)
grid on
